<p>
  <%= t('devise.mailer.reset_password_instructions.greeting', :recipient => @resource.nickname, :default => "Hi #{@resource.nickname}!") %>
</p>
<p>
  <%= t('devise.mailer.reset_password_instructions.instruction', :default => "You requested to reset your password for your account. Click the link below to reset it.") %><br><br>
  <a href="<%= edit_password_url(@resource, :reset_password_token => @token, locale: I18n.locale) %>" target="_blank"><%= edit_password_url(@resource, :reset_password_token => @token, locale: I18n.locale) %></a>
</p>
<p>
  <%= t('devise.mailer.reset_password_instructions.instruction_4', :default => "Your password won't change until you access the link above and set a new password. If you did not request a password reset, please ignore this email or replay to let us know.") %>
</p>
