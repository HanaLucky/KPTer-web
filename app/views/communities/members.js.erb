$(function () {
  $('#modal-button-member').html('<%= j(render(:partial => 'members', :locals => { current_community: @community, members: @members })) %>');
  // modal settings
  MicroModal.init({
    disableScroll: true
  });
  MicroModal.show('modal-button-member');
  // ...を機能させるため
  componentHandler.upgradeAllRegistered();
});
