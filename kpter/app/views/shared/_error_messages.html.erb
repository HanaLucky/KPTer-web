<% if object.errors.any? %>
  <script>
    toastr.options = {
      "closeButton": true,
      "timeOut": "0",
      "extendedTimeOut": "0",
    }
    toastr.warning('<ul><%= raw(resource.errors.values.map { |k, v| content_tag(:li, k) }.join) %></ul>', 'This form contains <%= pluralize(object.errors.count, 'error') %>.')
  </script>
<% end %>
