<!-- Navigation -->
<nav class="navbar navbar-default navbar-static-top" role="navigation" style="margin-bottom: 0">
  <div class="container-fluid">
    <div class="navbar-header">
        <a class="navbar-default navbar-brand page-scroll header-text title" style="color: #21b7b6;" href="/">KPTer</a>
    </div>
    <!-- /.navbar-header -->

    <ul class="nav navbar-nav navbar-right">
        <li>
            <a class="page-scroll header-text" href="#about">ABOUT KPTer</a>
        </li>
        <li>
            <a class="page-scroll header-text" href="#services">About KPT</a>
        </li>
        <% unless user_signed_in? %>
          <li>
              <a class="page-scroll header-text" href="<%= url_for new_user_session_path %>"><span class="fa fa-sign-in"></span>Login</a>
          </li>
        <% else %>
          <li>
              <a class="page-scroll header-text" href="/users/edit"><span class="fa fa-user fa-fw"></span>User Profile</a>
          </li>
          <li>
            <a class="page-scroll header-text" href="<%= url_for destroy_user_session_path %>"><span class="fa fa-sign-out"></span>Logout</a>
          </li>
        <% end %>
    </ul>
  </div>
    <!-- /.navbar-top-links -->
    <% if user_signed_in? %>
      <% if controller.controller_name == "mypages" %>
        <%= render :partial => "side_column", :locals => { communities: @communities } %>
      <% elsif controller.controller_name == "communities" %>
        <%= render :partial => "side_column", :locals => { communities: @communities, new_community: @communities, current_community: @community } %>
      <% end %>
    <% end %>
</nav>
